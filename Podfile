# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Whooosh' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Whooosh
  pod 'Mapbox-iOS-SDK'
  pod 'Arrow'
  pod 'ClusterKit'
  pod 'FFPopup'
  pod 'SwiftyJSON'
  pod 'FontAwesome.swift'
  pod 'Kingfisher'

  target 'WhoooshTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WhoooshUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
